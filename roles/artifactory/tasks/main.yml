- name: set artifactory prompt
  ansible.builtin.shell: set-prompt artifactory

#- name: setup artifactory repo
#  ansible.builtin.template:
#    src: artifactory.repo
#    dest: /etc/yum.repos.d/jfrog-artifactory-rpms.repo

- name: Download artifactory
  ansible.builtin.unarchive:
    src:  https://releases.jfrog.io/artifactory/bintray-artifactory/org/artifactory/oss/jfrog-artifactory-oss/7.71.11/jfrog-artifactory-oss-7.71.11-linux.tar
    dest: /opt
    remote_src: yes

#- name: Install jfrog
#  ansible.builtin.dnf:
#    name: jfrog-artifactory-oss
#    state: installed


